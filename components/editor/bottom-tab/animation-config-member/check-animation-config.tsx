import React from "react";
import CheckButton from "../../../common/check-button";

const CheckAnimationConfig: React.FC = () => {

  const onPress = () => {
    // sth
  };

  return <CheckButton 
    onPress={onPress}
  />;
};

export default CheckAnimationConfig;
