import React from "react";
import XButton from "../../../common/x-button";

const XCreate: React.FC = () => {

  const onPress = () => {
    // sth
  };

  return <XButton 
    onPress={onPress}
  />;
};

export default XCreate;
